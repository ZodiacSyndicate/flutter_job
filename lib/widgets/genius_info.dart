import 'package:flutter/material.dart';

class GeniusInfo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('geniusinfo'),
      ),
    );
  }
}
